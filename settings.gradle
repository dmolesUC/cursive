include 'cursive-inv-core', 'cursive-inv-api', 'cursive-inv-store', 'cursive-inv-test'

    rootProject.children.each { project ->
      print project
      project.buildFileName = "${project.name}.gradle"
}
